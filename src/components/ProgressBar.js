import * as React from 'react';
import { ProgressBar, Colors } from 'react-native-paper';

const Progresso = () => (
  <ProgressBar style={{marginTop: 3, borderRadius: 5}}progress={0.5} color="#007CE0" />
);

export default Progresso;
